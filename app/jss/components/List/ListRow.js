export const colorBarStyleheet = (theme) => ({
  rowColorBar: {
    position: 'absolute', 
    left: 0, 
    width: 3, 
    top: 0, 
    bottom: 0,
    backgroundColor: theme.colors.primary.mainBlue,
  },
});

export const checkboxStylesheet = (theme) => ({
  checkbox: {
    width: 40,
    height: 40,
  },
});