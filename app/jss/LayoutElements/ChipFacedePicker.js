export const styleSheet = theme => ({
  inputAdornmentIcon: {
    fontSize: 16,
    marginTop: 4,
    color: theme.colors.primary.mediumGrey
  },

  emptyField: {
    display: "flex"
  },

  label: {
    marginTop: -3
  },

  input: {
    marginTop: 4
  },

  chip: {
    margin: "0 8px 6px 0"
  }
});
