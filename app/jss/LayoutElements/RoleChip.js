export const styleSheet = theme => ({
  chip: {
    margin: "0 8px 8px 0"
  },

  roleType: {
    padding: "0 8px",
    fontWeight: 500,
    lineHeight: "32px",
    textAlign: "center",
    borderTopLeftRadius: "16px",
    borderBottomLeftRadius: "16px",
    color: "white"
  }
});
